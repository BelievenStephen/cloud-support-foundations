## Mar 18, 2026

## ALB 5xx Triage

### CloudWatch metrics — `AWS/ApplicationELB`

| Metric | What it measures |
|--------|-----------------|
| `RequestCount` | Requests counted only after the ALB successfully chooses a target — rejected requests before target selection are not included |
| `TargetResponseTime` | Time in seconds from when the request leaves the ALB until the target begins sending response headers — target-side latency only, not total page load time |
| `HTTPCode_ELB_5XX_Count` | 5xx responses generated by the ALB itself |
| `HTTPCode_Target_5XX_Count` | 5xx responses generated by the targets |
| `HealthyHostCount` | Number of targets currently considered healthy in the target group |

---

### Triage pattern

| Observation | Start here |
|-------------|------------|
| ELB 5xx up, Target 5xx flat | ALB side — listener/routing behavior, connection resets, malformed responses, handshake issues |
| Target 5xx up | App/target side — app logs, process health, recent deploys, dependency failures, resource pressure |
| HealthyHostCount down | Target health — health check path, success codes, app port, SG/NACL rules, timeout behavior |

---

### 502 vs 503

**503 — think target availability first:**
- No registered targets
- All targets unhealthy
- Targets in an unusable state

**502 — think broken ALB-to-target response path first:**
- Connection reset by target
- Malformed or oversized response headers
- SSL handshake failure
- Target closing the connection early

> Don't assume 502 means only unhealthy targets — it points to how the ALB and target are communicating, not just whether the target is up.

---

### Reading a 5xx spike

**`HTTPCode_ELB_5XX_Count` rises, `HTTPCode_Target_5XX_Count` flat:**
- The ALB itself is generating the errors
- Investigate ALB behavior, listener/rule configuration, and ALB-to-target connection handling

**`HTTPCode_Target_5XX_Count` rises:**
- The targets are returning server errors
- Check app logs, app health, recent changes, and backend dependencies

**`HealthyHostCount` drops at the same time:**
- Check target health before anything else
- Health check failures can drive request failures even when `RequestCount` looks normal

---

### Quick reference

- **ELB 5xx up, Target 5xx flat** → start on ALB side
- **Target 5xx up** → start on app/target side
- **HealthyHostCount down** → start on health checks and target availability
- **503** → no usable targets
- **502** → bad ALB-to-target connection or bad target response

---
